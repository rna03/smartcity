# Local development template. Copy to .env and replace placeholder values.
# Never use these placeholder values as production credentials.
POSTGRES_DB=smartcity
POSTGRES_USER=smartcity
POSTGRES_PASSWORD=change_me
ConnectionStrings__SmartCityDatabase=Host=localhost;Port=5432;Database=smartcity;Username=smartcity;Password=change_me
