# Database

Database seed/import scripts will live here. Real and synthetic datasets will be
kept in separate subdirectories and documented when they are added.

The PostgreSQL/PostGIS development database is currently defined in the root
`docker-compose.yml` file.
